server:
  port: 8762
spring:
  datasource:
   url: jdbc:mysql://127.0.0.1:3306/blgmanager?useUnicode=true&characterEncoding=utf-8
   username: root
   password: root
   driver-class-name: com.mysql.jdbc.Driver

mybatis:
    mapper-locations: classpath:mapper/*.xml
    type-aliases-package: com.arryluo.springbootmybatisplus.entity